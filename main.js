
function setInfo(first_name,
    last_name,
    age,
    dob,
    fav_food,
    fa_movie) {
    this.first_name = first_name;
    this.last_name = last_name;
    this.age = age;
    this.dob = dob;
    this.fa_movie = fa_movie;
    this.fav_food = fav_food;
}

let saja = new person("Saja", "AlMassimi", 22, "5/3/2002", ["Divergent", "Hunger Games"], ["Shawerma", "Bamia"]);

// **********************************************************************************

/*
 */
function firstName(person) {
    for (let index = 0; index < array.length; index++) {

    }
}